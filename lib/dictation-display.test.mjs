import test from 'node:test';import assert from 'node:assert/strict';import {createJiti} from 'jiti';const {parseDictationSpans}=await createJiti(import.meta.url).import('./dictation-display.ts');
test('stable-word spans retain exact Unicode spacing and punctuation status',()=>{const spans=[{text:'Hello',kind:'word',stable:true},{text:',',kind:'punctuation',stable:false},{text:'  ',kind:'spacing',stable:false},{text:'🦊',kind:'word',stable:true}];assert.deepEqual(parseDictationSpans(spans,'Hello,  🦊'),spans);});
test('malformed or mismatched annotations fall back to authoritative text',()=>{assert.equal(parseDictationSpans([{text:'wrong',kind:'word',stable:true}],'correct'),undefined);assert.equal(parseDictationSpans([{text:'x',kind:'html',stable:true}],'x'),undefined);assert.equal(parseDictationSpans([{text:'x',kind:'word',stable:'true'}],'x'),undefined);});
test('clean-tail text spans preserve interior punctuation without marking it as punctuation',()=>{const spans=[{text:'Hello, world',kind:'text',stable:false}];assert.deepEqual(parseDictationSpans(spans,'Hello, world'),spans);});
